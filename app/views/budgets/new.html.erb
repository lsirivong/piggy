<% if current_user.budgets.none? %>
<div id="notice-wrap">
  <p>It looks like you haven't created any budgets yet. Use this form to set up your first budget to get started using Piggy.</p>
</div>
<% end %>
<h2>New budget</h2>

<%= render 'form' %>

<% if current_user.budgets.any? %>
  <%= link_to 'Cancel', :root %>
<% end %>
