<h1>Dashboard</h1>
<div class="envelopes">
  <h2>Envelopes</h2>
  <% @envelopes.each do |e| %>
    <div class="envelope" id=<%= "envelope_#{e.id}" %>>
      <%= render e %>
    </div>
  <% end %>
</div>
<div class="goals">
  <h2>Goals</h2>
  <%= render @goals %>
</div>